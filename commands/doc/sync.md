# /doc sync - 同步索引与文件系统

手动触发 INDEX.md 与 doc/ 目录的对账同步。

## 使用方法

```bash
/doc sync
```

## 执行流程

1. 扫描 `doc/` 目录下所有主题文件夹
2. 对比 INDEX.md 中的条目
3. 清理 INDEX.md 中已不存在的主题
4. 补充 INDEX.md 中缺失的主题（从文件夹推断）
5. 输出同步报告："已同步索引 - 移除 N 个，补充 M 个"

## 使用场景

- 手动删除了主题文件夹后
- INDEX.md 与实际目录不一致时
- 定期维护索引准确性
